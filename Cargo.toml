[package]
name = "taskboard"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
axum = { version = "0.6.6", optional = true }
http = { version = "0.2.8", optional = true }
leptos = { version = "0.1.3", default-features = false, features = ["stable"] }
leptos_axum = { version = "0.1.3", optional = true }
once_cell = { version = "1.17.0", optional = true }
serde = "1.0.152"
serde_json = "1.0.93"
tokio = { version = "1.25.0", features = ["rt-multi-thread", "macros"], optional = true }
tower-http = { version = "0.3.5", features = ["fs"], optional = true }
uuid = { version = "1.3.0", features = ["v4", "js", "serde"] }
wasm-bindgen = "0.2.84"

[features]
default = []
csr = ["leptos/csr"]
hydrate = ["leptos/hydrate"]
ssr = ["leptos/ssr", "dep:axum", "dep:tokio", "dep:http", "dep:tower-http", "dep:once_cell", "dep:leptos_axum"]
