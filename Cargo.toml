[package]
name = "taskboard"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
leptos = { version = "0.0.17", features = ["stable"] }
uuid = { version = "1.2.2", features = ["v4", "js"] }
